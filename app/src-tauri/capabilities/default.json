{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the manager and launcher windows",
  "windows": ["main", "launcher"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
